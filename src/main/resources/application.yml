spring:
    batch:
        job:
            names: ${job.name:chunkProcessingJob}
    datasource:
        driver-class-name: org.h2.Driver
    sql:
        init:
            data-locations: classpath:person.sql
